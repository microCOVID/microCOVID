name: Update COVID prevalence data

on:
  schedule:
    # 7:00am Pacific = 14:00 UTC
    - cron:  '0 14 * * *'
  workflow_dispatch:

jobs:
  prevalence:
    # TODO(blanchardjeremy): This whole file is a work in progress.
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - uses: actions/setup-python@v2

    - uses: syphar/restore-virtualenv@v1
      id: cache-virtualenv
      with:
        requirement_files: requirements-manual.txt

    - uses: syphar/restore-pip-download-cache@v1
      if: steps.cache-virtualenv.outputs.cache-hit != 'true'

      # the package installation will only be executed when the
      # requirements-files have changed.
    - run: pip install -r requirements-manual.txt
      if: steps.cache-virtualenv.outputs.cache-hit != 'true'

    - name: Run update preavlence
      env:
        CAN_API_KEY: ${{ secrets.COVID_ACT_NOW_API_KEY }}
        TEST_VARIABLE: test
      run: |
        echo "$TEST_VARIABLE"
        CAN_API_KEY="$CAN_API_KEY" python3 update_prevalence.py

    # TODO: Delete branch we're going to use in case it exists

    # Checkout a separate branch
    
    # Run update_prevalence.py including the CAN API KEY
    
    # Run linter (may need to set up all of yarn first?)

    # Commit the updated files with the current date
    # Create a pull request
    # Auto merge the pull request

    
