import { PostMap } from 'posts/post'

import post1 from './ux-redesign'

// Note: the keys in this map are the public URL for the post. Changing them will break links.
export const pages: PostMap = {
  '1-uxredesign': post1,
}
